language: python
python:
  - "3.6"
  - "3.7"
dist: xenial
sudo: true
jobs:
  include:
    - name: pytest
      script: pytest
    - name: pylint
      script: pylint src
install:
  - pip install -r requirements.txt
  - pip install pytest pylint
